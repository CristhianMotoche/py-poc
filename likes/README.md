# Likes

Simple App to track likes of people.

## Requirements

* asdf

## Start

```
asdf install
python -v venv venv
source venv/bin/activate.fish
pip install -r requirements.txt
prisma db push
```
